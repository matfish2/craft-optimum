{% extends "_layouts/cp" %}

{% set title = "Activity Logs Settings"|t('app') %}

{% set crumbs = [
    { label: "Settings"|t('app'), url: url('settings') },
] %}

{% set navItems = {
    settings: { label: "Settings"|t('app'), url: url('settings/activity-logs') },
    actions: { label: "Actions"|t('app'), url: url('settings/activity-logs/actions') },
} %}

{% set docTitle = navItems[selectedNavItem].label~' - '~title %}

{% block sidebar %}
    {% include "_includes/nav" with { items: navItems, selectedItem: selectedNavItem } only %}
{% endblock %}
